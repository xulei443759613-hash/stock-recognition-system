# 2026-07-01

## 本次完成

- 验证 Codex/Git/GitHub 修复后，项目命令已能继续执行。
- 完成短线模式收尾：4-5 日训练计划、训练仓上限、单笔最大亏损、一手门槛、5%/8%/10% 目标价、5 日退出规则。
- 接入双日线数据源：`EastMoneyDailyDataProvider` 优先，`TencentDailyDataProvider` 兜底，可拉取最近 N 个交易日收盘价、最新涨跌幅、换手率，并写入 `MarketEvidence`。
- CLI `review` 支持 `--auto-market-data --history-days 20`，自动从群消息中识别 6 位股票代码并补充日线行情；`--auto-eastmoney` 保留为兼容参数。
- 新增东方财富和腾讯行情解析单元测试，避免字段位置变化导致收盘价、涨跌幅、换手率误读。
- 增加复盘闭环：`outcome` 命令记录真实结果，`source-score` 命令统计群源质量。
- 增加腾讯分时分钟线：有 `push-date/push-time` 时自动还原消息发出时价格、当时涨跌幅和涨停状态。

## 短线模式边界

- 只适合训练仓，不适合满仓或重仓。
- 对 34,000 元账户，默认训练仓为 3,400 元，单笔最大计划亏损为 170 元。
- 只有主系统结论为“小仓位试错”且风险收益结构达标时，短线计划才允许真实买入。
- 群消息、截图、老师话术都只作为线索；行情、公告、财报、交易所和可信数据源优先。

## 常用命令

```powershell
python -m compileall stock_recognition_system examples tests
python -m unittest discover -s tests
python -m stock_recognition_system.cli review `
  --message-file examples/group_message.txt `
  --push-date 2026-06-30 `
  --push-time 11:00 `
  --auto-market-data `
  --history-days 20 `
  --account-value 34000
python -m stock_recognition_system.cli outcome `
  --stock-code 603991 `
  --source group `
  --action 放弃 `
  --push-date 2026-06-29 `
  --push-time 14:50 `
  --target-price 138.30 `
  --stop-loss 113.10 `
  --max-price 141.15 `
  --min-price 121.88
python -m stock_recognition_system.cli source-score --source group
```

## 证据采集剧本

- 新增 `evidence-plan` 命令，收到群消息后可先输出应查来源、采集字段、通过标准和否决标准。
- 把基金/社保、游资控盘、财务改善、现金流、评级、题材等话术映射到不同核验路径。
- 每条消息默认加入 P0 证据：消息时点价格、20日价格结构、账户承受力。
- 东岳硅材 300821 样本验证通过：10:57 消息时点价格 21.37，结论仍为放弃，原因是风险话术、盈亏比不足和 100 股止损风险超训练仓单笔上限。
- 当前行情链路：东方财富接口仍被远端断开，腾讯分时和腾讯日线可用，系统按设计降级并写入数据警告。

## 机会评级和错失复盘

- 新增机会评级：真实仓位结论不放松，但报告会区分“可小仓”“模拟跟踪”“等待更优价格”“补证据观察”“剔除机会”。
- 对盈亏比不足的样本，不再只写放弃；会反推普通风控最高买入价、短线最高买入价、100 股止损风险最高买入价和训练模式综合可执行价。
- 东岳硅材样本：21.37 买入不划算，但不是永久删除机会；短线训练综合可执行价为 20.50，需要约 4.07% 回撤后才重新评估。
- `outcome` 和 `source-score` 增加错失机会统计：可执行错失、非可执行上涨、顺序待查。
- 产品原则：放弃后上涨不等于系统错；只有曾回到可执行价且未先触发止损，再触达目标，才算可执行错失。

## 系统建议止盈止损

- 新增 `exit_suggestion.py`，综合参考买入价、原始目标/止损、账户亏损上限、近期支撑和波动，输出一个建议止盈价和一个建议止损价。
- 报告新增“系统建议止盈止损”章节，关键价位区也直接展示系统建议止盈价、系统建议止损价和建议盈亏比。
- 东岳硅材样本在等待价 20.50 口径下，系统建议止盈价为 22.14，建议止损价为 19.48，建议盈亏比约 1.61。
- 如果当前不允许真实仓位，建议价只用于等待条件和模拟复盘，不作为追入指令。
